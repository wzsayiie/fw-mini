#!/bin/zsh

set -e -u

cd `dirname $0`

nam="libmini.dylib"
src="appsrc/m*.cpp host_osx/mini/*.mm"
inc="-Iappsrc"
ref="-framework Cocoa -framework JavaScriptCore"
flg="--std=c++17 -fobjc-arc -dynamiclib"

mkdir   -p BUILD
rm      -f BUILD/$nam
clang++ -o BUILD/$nam $inc $ref $flg $src
